server:
  port: 8084
  tomcat:
    uri-encoding: UTF-8

spring:
  application:
    name: chat-service

  kafka:
    bootstrap-servers: ${KAFKA_SERVER_ADDR}:${KAFKA_SERVER_PORT}

  cloud:
    nacos:
      server-addr: ${NACOS_SERVER_ADDR}:8848
      config:
        group: DEFAULT_GROUP
        file-extension: yaml
        encode: UTF-8
        namespace: ${NACOS_NAMESPACE}
      discovery:
        namespace: ${NACOS_NAMESPACE}
  config:
    import:
      - file:.env[.properties]
      - nacos:common-spring.yaml
      - nacos:common-logs.yaml
      - nacos:common-mybatis.yaml
      - nacos:common-redis.yaml
      - nacos:chat-service-dubbo.yaml

sh:
  redis:
    host: ${REDIS_HOST}
    port: ${REDIS_PORT}
    password: ${REDIS_PASSWORD}
  jdbc:
    host: ${MYSQL_SERVER_ADDR}
    password: ${MYSQL_ROOT_PASSWORD}
    database: sh_chat
  nacos:
    server-addr: ${NACOS_SERVER_ADDR}
    namespace: ${NACOS_NAMESPACE}
  ws:
    max-message-length: 2000
    # WebSocket 握手允许的 Origin（逗号分隔，支持 * 通配）。仅浏览器客户端受此约束。
    allowed-origin-patterns: "http://localhost:*,http://127.0.0.1:*,https://sweethome.asia,https://*.sweethome.asia"

jwt:
  public-key: ${JWT_PUBLIC_KEY}

logging:
  level:
    asia.sweethome: debug

