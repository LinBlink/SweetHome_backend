<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="asia.sweethome.moment.mapper.MomentLikerMapper">

    <update id="likeMoment">
        insert into moment_liker ( moment_id, liker_user_id, like_count, created_at) values (
                                                                                             #{momentId}, #{likeUserId}, 1, now()
                                                                                            )
        on duplicate key update like_count = like_count + 1
    </update>

    <select id="getMomentLikeCount" resultType="java.lang.Integer" parameterType="java.lang.Long">
        select coalesce(sum(like_count), 0)  from moment_liker
                 where moment_id = #{momentId}
    </select>


</mapper>
